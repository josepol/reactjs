import React from 'react';
import { Menu } from '../../components/menu';
import './Profile.scss';

const Profile = () => (
    <div>
        <Menu />
        <h1>Profile</h1>
    </div>
)

export default Profile;